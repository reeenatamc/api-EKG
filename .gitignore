__pycache__/
*.py[cod]
.venv/
venv/

.env
db.sqlite3

# Uploaded ECG images and everything the pipeline writes from them. These are clinical
# images of real recordings: they must never reach a remote.
media/
work/

# collectstatic's output (docker/entrypoint-api.sh) and scripts/backup.sh's default
# destination -- both generated, and the second one is a database dump plus real images.
staticfiles/
backups/

.DS_Store
.idea/
.vscode/
